info:
  name: profile
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3000/auth/profile
  body:
    type: json
    data: |-
      {
        "username": "khalid123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
